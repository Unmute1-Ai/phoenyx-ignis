#!/bin/sh
set -eu

if ! command -v ignis >/dev/null 2>&1; then
  python3 -m pip install --break-system-packages --no-deps /opt/phoenyx-ignis
fi

printf '\nPhoenyx Ignis recovery environment\n'
printf 'Start with: ignis twin --output /tmp/machine-twin.json\n\n'
